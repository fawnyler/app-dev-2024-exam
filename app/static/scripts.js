var easyMDE = new easyMDE({
  element: document.getElementById('descr'),
  spellChecker: false, // Optional: disable spell checker
  forceSync: true // Optional: force text to be updated immediately in the textarea
});